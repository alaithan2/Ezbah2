rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // EMERGENCY DIAGNOSTIC: Open access for all authenticated users to everything
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

    match /_system_/health-check {
      allow get: if true;
    }
  }
}
